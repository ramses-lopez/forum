<!DOCTYPE html>
<html>
  <head>
    <title>LibreForum</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="container-fluid">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <%= link_to 'LibreForum', root_path, class: 'navbar-brand' %>

      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <% if current_user.nil? %>
            <li><%= link_to 'Sign up', new_user_path, class: 'nav-link' %></li>
            <li><%= link_to 'Sign in', new_user_session_path, class: 'nav-link' %></li>
          <% else %>
            <li><%= link_to 'Topics', topics_path, class: 'nav-link' %></li>
            <li><%= link_to 'Sign out', user_session_path(current_user.id), method: :delete, class: 'nav-link' %></li>
          <% end %>
        </ul>
      </div>
    </nav>

    <% flash.each do |key, value| %>
  		<div class="alert alert-<%= key %>">
  			<%= value %>
  		</div>
  	<% end %>

    <%= yield %>

  </body>
</html>
